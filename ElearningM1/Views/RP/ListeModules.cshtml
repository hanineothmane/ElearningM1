
@{
    ViewBag.Title = "ListeModules";
}
<head>
    <link rel="stylesheet" media="screen" type="text/css" title="Exemple" href="~/Content/css/materialize.css" />
    <link rel="stylesheet" media="screen" type="text/css" title="Exemple" href="~/Content/css/materialize.min.css"/>
    <link rel="stylesheet" media="screen" type="text/css" title="Exemple" href="~/Content/css/style.css" />
</head>
<h2>Les modules</h2>
<body>
    <table class="striped">
        <thead>
            <tr>
                <th>Nom</th>
                <th>Coefficient</th>
                <th>Est-il national ?</th>
            </tr>
        </thead>
        @foreach (ElearningM1.Models.Module module in ViewData["Modules"] as List<ElearningM1.Models.Module>)
        {
            <tr>
                <td>@Html.ActionLink(module.Nom, "ChercheModule", "RP", new { nom = module.Nom }, null)</td>
                <td>@module.Coeff</td>
                @if (module.EstNational)
                {
                    <td>Oui</td>
                }
                else
                {
                    <td>Non</td>
                }
            </tr>
        }
    </table>
</body>
